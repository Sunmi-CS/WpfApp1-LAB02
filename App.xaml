<Application x:Class="WpfApp1.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfApp1"
             xmlns:vm="clr-namespace:WpfApp1.ViewModels"
             xmlns:views="clr-namespace:WpfApp1.Views">

    <Application.Resources>

        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>

                <ResourceDictionary
                    Source="Resources/Colors.xaml"/>

                <ResourceDictionary
                    Source="Resources/Styles.xaml"/>

            </ResourceDictionary.MergedDictionaries>


            <!-- INGRESOS -->

            <DataTemplate DataType="{x:Type vm:IngresoViewModel}">
                <views:IngresoView/>
            </DataTemplate>

            <!-- CONDUCTORES -->

            <DataTemplate DataType="{x:Type vm:ConductoresViewModel}">
                <views:ConductoresView/>
            </DataTemplate>

            <!-- (DataTemplates para otras vistas gestionadas dinámicamente desde MenuViewModel) -->

            <!-- REPORTE INGRESOS -->

            <DataTemplate DataType="{x:Type vm:ReporteIngresosViewModel}">
                <views:ReporteIngresosView/>
            </DataTemplate>

        </ResourceDictionary>

    </Application.Resources>

</Application>
